// SAFE STUB — restored automatically whenever a generated bespoke scene fails.
// scene_coder.py overwrites Bespoke.tsx with a topic-specific animated layer per video.
import React from 'react';
import type {Segment} from './Short';

const BespokeOverlay: React.FC<{
  segments: Segment[];
  skin: {bg: string; accent: string};
}> = () => null;

export default BespokeOverlay;
